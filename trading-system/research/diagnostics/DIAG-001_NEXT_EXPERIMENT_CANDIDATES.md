# DIAG-001: 次の実験候補（最大3件、診断結果に基づく）

`DIAG-001_ENTRY_EXIT_ANALYSIS.md`の判定（**Stage2完了、Primary Bottleneck=MIXED、
Confidence=MEDIUM-HIGH**。ENTRY・EXIT〔利益保護の不在〕の両方に独立した根拠あり）に基づき、
次の候補を優先順位付きで提示する。**本ファイルはすべて候補であり、まだ新規`experiment_id`は
発行していない。** 承認された候補から1件ずつ、`BACKTEST_COVERAGE_EXPANSION_PLAN.md`の運用
ルールに従って正式発行する。

パラメータ最適化・複数フィルターの総当たりは含めない。各候補は変更点を1つに限定する。

## 候補1: DIAG-001 Stage2の実行（**完了、2026-08-10**）

- **原因への近さ**: 最も近い。Entry・Exitの両論点をMFE/MAE実測により判定した
  （`DIAG-001_ENTRY_EXIT_ANALYSIS.md`「判定」節参照）
- **結果**: ENTRY側（損切りの約30%がMFE_R<0.25で反転、売りは35〜37%とさらに高い）とEXIT側
  （損切りの約24%が+1R以上の含み益から全損に反転、利益保護機構が構造的に皆無）の両方が
  独立して確認された。TP幅自体は妥当（勝ちの含み益捕捉率87〜91%）
- **本候補は完了。以降の候補2・3の優先順位判断の前提として使用する**

## 候補2: Buy only / Sell only 比較実験（`EXP-005`候補）

- **原因への近さ**: DIRECTION_ASYMMETRYはStage1で**HIGH confidence**で確認済み（Rベース正規化後も
  買いPF≈1.03・売りPF≈0.40の非対称性が残存）。Stage2の結果を待たずに独立して検証可能
- **変更点**: `EnableLong`/`EnableShort`のみ（コード変更不要、EA入力パラメータの変更のみ。
  `BACKTEST_COVERAGE_EXPANSION_PLAN.md`優先3で検証済み、CC001・CC002双方に既存の入力パラメータ
  として存在することを確認済み）
- **1実験1変更点**: 遵守（`EnableShort=false`と`EnableLong=false`は別々の実験として登録し、
  同時に変更しない）
- **OOS検証可能**: 可能（`EXP-004`のCC001確定Baselineが得られ次第、同一期間で実施すればOOSではなく
  同一期間内の診断になる。真のOOSにするには別期間データが必要、`BACKTEST_COVERAGE_EXPANSION_PLAN.md`
  優先4参照）
- **経済的意味**: あり（売り側の非対称性が「売買判定固有の欠陥」なのか「たまたまこの期間の
  USDJPYが上昇バイアスだった」だけなのかを診断できる。ただし**「売りロジックを停止すべき」という
  Decisionを先取りしない**。あくまで診断目的の比較として位置づける（ユーザー指定の通り）
- **過剰最適化回避**: 2値（Long Only / Short Only）の比較のみで、閾値探索や複数パラメータの
  同時調整は行わない

## 候補3: ポジションサイジング歪みの深掘り分析（既存データの追加解析、新規バックテストなし）

- **原因への近さ**: POSITION_SIZINGはStage1で**副次的要因（MEDIUM confidence）**と判定済み
  （$ベースPFとRベースPFの差は0.06〜0.09で、主因ではない）。優先度は候補1・2より低い
- **変更点**: なし（新規バックテスト不要。既存の183+195トレードを、キャップの強さ
  （`理論ロット / MaximumLot`の比率）で分位分割し、キャップが強いトレードほど実現Rが悪化するか
  どうかを追加集計する）
- **1実験1変更点**: 該当（分析のみで実験ではないが、追加する集計軸は1つに限定する）
- **OOS検証可能**: 該当なし（既存データの再解析のため）
- **経済的意味**: 限定的。仮にキャップの強さと成績に相関があっても、それ自体はPF<1.0を説明しない
  （Stage1で確認済み）ため、優先度は低いままとする
- **過剰最適化回避**: パラメータ変更を伴わないため該当リスクなし

## 明示的に候補化しなかったもの

- **ADX/ATRレジームフィルター、上位足確認、時間帯フィルター等のEntry系新機能**: Stage2でENTRYの
  弱さ自体は確認されたが、「どのフィルターが有効か」は本診断の対象外（診断は原因の特定までであり、
  解決策の検証ではない）。次のHypothesis候補として別途提案が必要
- **建値移動・トレーリング・部分利確等のExit保護機構の追加**: Stage2で最も具体的な根拠
  （損切りの約24%が+1R以上から全損に反転）が得られたが、**これはEAコードの変更を伴う**ため、
  ユーザーの明示的な禁止事項（「まだEAコードを変更しないでください」）に従い、本ファイルでは
  候補化しない。`DIAG-001_ENTRY_EXIT_ANALYSIS.md`に事実として記録済み。実装を検討する場合は、
  別途Hypothesis（`H004`候補等）として事前登録し、ユーザーの承認を得てから着手すること
- **Long/Short別パラメータの導入**: ユーザーの明示的な禁止事項に従い候補化しない（候補2はあくまで
  診断目的の停止/非停止比較であり、方向別に異なるTP/SL等を導入するものではない）

## 優先順位の要約（2026-08-10更新）

1. ~~候補1（Stage2実行）~~ **完了**（2026-08-10）
2. **候補2（Buy/Sell比較、`EXP-005`）— 次に正式登録する候補**。Stage2完了により、
   「Buy onlyの結果だけではEntry/Exitどちらが原因か説明できない」というユーザー懸念の前提
   （Entry/Exit診断）が満たされたため、逐次実施の方針通り本候補へ進む
3. 候補3（サイジング深掘り）— 優先度低（副次的要因と判定済みのため）のまま据え置き

## created_at / updated_at

- created_at: 2026-08-02
- updated_at: 2026-08-10（Stage2完了を反映。候補1completed、候補2〔EXP-005〕を次の正式登録対象に）
