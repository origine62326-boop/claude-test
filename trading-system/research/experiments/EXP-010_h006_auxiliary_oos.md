# EXP-010: H006（トレンド成熟度フィルター）の追加補助OOS検証

`EXPERIMENT_TEMPLATE.md`に基づく実験記録。**H006の追加OOS検証。`EXP-009`（第1OOS期間）で
Primary Metricは達成したものの買い取引数のGuardrailを満たせず`HOLD`となったため、別期間での
再現性を確認する。**

## 本実験の位置づけ（重要、最初に明記する）

**本実験は`H006`の最終採否を決定するものではなく、「追加補助OOS」として扱う**（ユーザー指示
2026-08-15）。理由は下記「テスト期間」の通り、利用可能なデータが約6ヶ月分しかなく、
`EXP-009`（1年間）と比べて期間が短いためである。

したがって本実験の結果がどうであれ、それ単独で`H006`を`ADOPTED`にすることはない。
**確認したいのは主に「効果の方向性が別期間でも再現するか」であり、採否そのものではない。**

## 基本情報

| フィールド | 内容 |
|---|---|
| experiment_id | EXP-010 |
| hypothesis_id | H006（トレンド成熟度による買いエントリー除外仮説） |
| title | `MaxBuyTrendDurationBars=50`の効果を、第2のOOS期間（約6ヶ月）で追加確認する |
| evidence_level | H |
| objective | `EXP-009`で確認された買い期待値Rの改善方向が、さらに別の期間でも再現するかを確認する。取引数の制約から最終判定には使わず、方向性の補助的な確認に用いる |
| rationale | `EXP-009`（2024-07-21〜2025-07-20）ではPrimary Metric（B−A=+0.0596）が事前登録基準(+0.0286)の2倍以上を達成したが、買い取引数が42件で事前登録Guardrail(50件以上)を下回り`HOLD`となった。これは「効果が消失した」のではなく「サンプル不足で採用判断ができない」状態であるため、別期間での方向性確認が有用と判断した |
| evidence_ids | なし |
| features | 既存実装（`claude/ea-trend-maturity-filter`、コミット`ff37c85`）をそのまま使用。コード変更なし |
| target | 下記「acceptance_criteria」参照 |

## テスト期間とデータ可用性（実行前に確認済み）

| フィールド | 内容 |
|---|---|
| test_period | **2024-01-25 〜 2024-07-20**（約6ヶ月、3,029バー） |
| 当初の希望期間 | 2023-07-21〜2024-07-20（1年間）。**しかしMT4ヒストリーセンターに2023年のH1データが存在せず、ユーザーがダウンロードを試みたが取得できなかったため断念した** |
| データ欠落の実態 | `DS011`(H1価格履歴CSV)には**2016-08-26 → 2024-01-25 の間に約7年半のギャップ**が存在する。実際に利用可能な連続データは2024-01-25以降のみ |
| H006発見期間との関係 | `DS004`/`DS010`(2025-07-21〜2026-07-27)と**非重複** |
| `EXP-009`OOS期間との関係 | `DS013`/`DS014`(2024-07-21〜2025-07-20)と**非重複**。本期間はその直前にあたる |
| EMA200等の遡り計算 | テスト開始日より前に2,059バーが存在し、`HasSufficientHistory()`が要求する本数を十分に満たす |

## 事前に予想される取引数（結果を見る前の予測、判定基準ではない）

`EXP-009`のRun A（1年間、6,166バー）では買い91件だった。本期間は3,029バー（約49.1%）であり、
**単純比例ならAの買いは約45件、Bはさらにフィルターで削減され約21件程度**と予測される。

**すなわち、事前登録済みのGuardrail（買い取引数50件以上）は、A/Bとも達成できない可能性が
高いことを、結果を見る前の時点で認識している。** それでも本実験を実施する理由は、上記
「位置づけ」の通り採否判定ではなく方向性確認が目的だからである。**この予測を理由に
Guardrailを緩和することはしない**（`RESEARCH_RULES.md`第3節）。

## 比較する2条件（A/B、変更点はH006のON/OFFのみ）

| | A（ベースライン） | B（H006あり） |
|---|---|---|
| MaxBuyTrendDurationBars | 0（無効） | 50 |
| EnableLong / EnableShort | true / true | true / true |
| その他 | `EXP-002`/`EXP-008`/`EXP-009`と完全一致（`MaxDailyLossPercent=2`, `MaxConsecutiveLosses=3`, `TradingStartHour=8`/`TradingEndHour=22`等） | 同左 |
| 初期証拠金・時間足・スプレッドモデル | 100000, H1, 現在値(5)固定（既存実験と完全一致） | 同左 |

**AとBの差分はEA入力パラメータの`MaxBuyTrendDurationBars`(0→50)のみ。**

## 事前登録した評価指標（既存定義を維持、結果を見た後に変更しない）

**`EXP-009`で使用した定義をそのまま引き継ぐ。数値基準は一切変更していない。**

### Primary Metric: 買い期待値R の改善幅

| 項目 | 内容 |
|---|---|
| 指標 | 買いサブセットの期待値R（実現R計算式、`EXP-006`〜`EXP-009`と同一）。B − A の差分 |
| 判定基準 | **B − A ≧ +0.0286**（`EXP-009`と同一。in-sample効果量+0.0571の50%という根拠も同一） |

### Secondary Metrics

| 指標 | 判定 |
|---|---|
| 買いPF（$ベース・Rベース） | B > A |
| 買い取引削減率 | `(A買い件数 − B買い件数) / A買い件数`。参考記録として必ず算出する（in-sample実績-36.9%、`EXP-009`実績-53.8%と比較する） |
| 全体PF、純損益 | 参考記録 |

### Guardrail Metrics（`EXP-009`と同一、緩和しない）

| 指標 | 基準 |
|---|---|
| 最大DD | B の最大DD(%) が A 以下（悪化しない） |
| 最大連敗数（Bのみ） | `acceptance_criteria.yaml`の`max_consecutive_losses_hard_limit=15`以内、かつ A + 2 以内 |
| 買い取引数（Bのみ） | **50件以上**（`EXP-009`と同一。上記の通り未達が予想されるが、基準は変更しない） |

### 追加確認項目（ユーザー指示による）

- 売りサブセットがA/Bで完全一致すること（`CheckSellSignal()`無変更の検証。`EXP-008`/`EXP-009`で
  確認済みの性質が本期間でも成立するか）

## 判定ルール（結果を見た後に変更しない）

本実験は「追加補助OOS」であり、単独で`ADOPTED`判定を出さない。結果に応じて以下のいずれかを記録する。

| 結果パターン | 判定・記録内容 |
|---|---|
| Primary達成（B−A ≧ +0.0286）かつ買い取引数50件以上 | `H006`の信頼度を再評価する。ただし本実験単独ではなく`EXP-009`と併せた総合評価とし、期間が短いことを`limitations`に明記した上で判断する |
| **Primary達成だが買い取引数50件未満** | `ADOPTED`にしない。`H006`は`HOLD`を維持。ただし**「追加OOSでも効果の方向性は支持されたが、サンプル不足のため採用判断はできない」と明記する**（ユーザー指示） |
| **効果が消失（B−A < +0.0286）または逆転（B < A）** | **`50 bars`という固定閾値の頑健性に疑義があることを明記する**。`H006`を再評価候補とし、`HYPOTHESIS_REGISTRY.md`にその旨を記録する（ユーザー指示） |
| Guardrail（DD・連敗）が悪化 | 逸脱内容を具体的に記録し、原因を分析する |

いずれの場合も、本実験の完了までは`DIAG-002`・新規Entryフィルター探索・`H007`の実装には
進まない（ユーザー指示）。

## required_data_sources（憲章第8節）

- USDJPY H1のMT4 Strategy Testerレポート（`.htm`形式、操作履歴込み）× 2件（A, B） — 必須
- 使用パラメータ一覧（A/Bとも） — 必須
- EA Commit SHA（`ff37c85`、既存ブランチを再利用するため新規実装は不要） — 必須
- Expertsログ（A, B各回。モデリング品質・エラー有無の確認のため） — 必須

## パラメータ・再現性情報

| フィールド | 内容 |
|---|---|
| parameters | A: `MaxBuyTrendDurationBars=0`。B: `MaxBuyTrendDurationBars=50`。他は`EXP-002`/`EXP-008`/`EXP-009`と同一 |
| random_seed | 対象外 |
| code_version | `ff37c85`（`claude/ea-trend-maturity-filter`、`EXP-008`/`EXP-009`と同一ビルド。新規コード変更なし） |
| data_version | A: `DS015`（登録済み）。B: 未確定（実行後に登録） |
| execution_date | A: 2026-08-15実施済み。B: 未実施（ユーザーによる新規バックテスト実行待ち） |

## 事前登録チェックリスト

- [x] test_periodを実行前に確定している（2024-01-25〜2024-07-20。発見期間・`EXP-009`期間の双方と非重複）
- [x] データ可用性を実行前に確認している（`DS011`のギャップを特定し、実行可能な期間を確定）
- [x] acceptance_criteria（Primary/Secondary/Guardrail）を`EXP-009`から変更せずに引き継いでいる
- [x] 取引数Guardrailが未達となる可能性を、結果を見る前に予測として明記している（緩和はしない）
- [x] 本実験が最終採否判定ではなく補助的な位置づけであることを明記している
- [x] 未来データを参照する特徴量が含まれていない
- [x] コード変更は不要であることを確認している（既存ブランチの再利用）
- [x] MT4での新規バックテスト実行A（ベースライン）完了。**B（`MaxBuyTrendDurationBars=50`）は未提出**（判定にはA/B両方が必要）

## 実行ログ（2026-08-15、Run A）

- inputs行: `MaxBuyTrendDurationBars=0`、他は`EXP-009`と同一設定を確認
- 初期化ログ: `...トレンド成熟度フィルター(H006/EXP-008)は買いのみMaxBuyTrendDurationBars=0
  (0=無効)で動作します。` — 正しいビルド・パラメータを確認
- **注意**: 提出されたExpertsログ（`9ee45d10-20260815.log`）は前半(3行目〜)が`EXP-009` Run Bの
  再掲、後半(**5691行目以降**)が本Run Aである。後半ブロックのみを採用した
- 実際のテスト範囲: 2024-01-25 00:00 〜 2024-07-19 23:00（**3,029バー**、事前登録時の想定と一致）
- ERROR 0件、`発注成功`101件（レポートの総取引数と一致）、日次損失上限・連敗制限とも0回発動、
  `EA停止 reason=0`で正常終了
- 本データを`DS015`として`DATASET_REGISTRY.md`へ登録した

### Aの結果集計

| 指標 | 全体 | 買いサブセット | 売りサブセット |
|---|---|---|---|
| 取引数 | 101 | **83** | **18** |
| PF（$ベース） | 1.17 | 1.258 | 0.963 |
| PF（Rベース） | - | 1.3825 | - |
| 勝率 | 40.59% | 40.96% | 38.89% |
| 純損益 | +1849.59 | +1959.77 | -110.18 |
| 期待値R（買い） | - | **+0.2262** | - |
| 最大DD | 1.99%(1989.09) | - | - |
| 最大連敗（全体） | 6 | 6 | 4 |
| モデリング品質 | 50.0%（`DS013`/`DS014`と一致） | - | - |

### 事前予測との差異（重要、記録として残す）

事前登録時に「Aの買いは約45件」と予測したが、**実際は83件**と大きく上回った。予測が外れた理由は、
バー数の単純比例（3,029/6,166=49.1%）で見積もったのに対し、**本期間は買い/売りの構成比が
`EXP-009`期間と大きく異なる**ためである（本期間: 買い83/売り18。`EXP-009`期間: 買い91/売り85）。
バー数あたりの買いシグナル発生密度そのものが本期間の方が高かった。

この結果、**事前に「達成できない可能性が高い」と予測していた買い取引数Guardrail(50件以上)は、
少なくともA側では達成している**。Bがフィルターで削減された後に50件を維持できるかが焦点となる
（`EXP-009`の削減率-53.8%をそのまま当てはめると約38件で未達、`EXP-008`のin-sample削減率-36.9%
なら約52件で達成、という境界線上）。

**予測が外れたこと自体は判定基準に影響しない**（Guardrailの数値は事前登録のまま変更しない）。

### 本期間の特性（判定とは別に記録）

売りがわずか18件・PF0.963とほぼ損益分岐であり、`DS004`(売りPF0.313で大幅赤字)・`DS013`
(売りPF1.149で黒字)のいずれとも異なる。3つの期間で売りの挙動が毎回異なっており、
**方向非対称性が期間依存である**という`EXP-009`時点の示唆をさらに補強する。これは将来の
`H007`（Short停止仮説）にとって重要な文脈情報である。

**Bの結果を受領し次第、Primary/Secondary/Guardrailの判定を追記する。**

## limitations（事前に予期される限界）

- **期間が短い（約6ヶ月）**: `EXP-009`の1年間と比べて統計的信頼性が低く、単独で採否判定に
  使えない。これが本実験を「補助」と位置づける最大の理由
- **取引数不足がほぼ確実**: 上記予測の通り、A・Bとも事前登録Guardrailの50件を下回る見込み
- **希望した期間が使えなかった**: 当初検証したかった2023-07-21〜2024-07-20はMT4に
  ヒストリーデータが存在せず、実行不可能だった。より長期・複数レジームでの検証は、
  ヒストリーデータの追加取得が実現するまで保留となる
- **統計的検出力の限界**: `EXP-009`と同じく、最低改善幅(+0.0286)は買いR値の標準誤差
  （`EXP-002`基準で約0.12）を大きく下回り、統計的有意性の主張ではなく実務的フィルターである
- **モデリング品質の不確実性**: 本期間のティックデータ密度は未検証（`EXP-009`期間では50.0%と、
  発見期間の57.79%より低かった）

## reviewer

未定（人間の承認者を今後指定）

## status

`DRAFT`（Primary/Secondary/Guardrail Metricsの事前登録は完了。EAコード変更は不要だが、
MT4での新規バックテスト実行〔ユーザー側〕が必要なため、実行結果受領まで`DRAFT`のまま）

## created_at / updated_at

- created_at: 2026-08-15
- updated_at: 2026-08-15（事前登録。`EXP-009`のHOLD判定を受けた追加補助OOSとして発行。
  当初希望period〔2023-07-21〜2024-07-20〕がデータ欠落により実行不可と判明したため、
  利用可能な2024-01-25〜2024-07-20に変更した上で登録）
- updated_at: 2026-08-15（Run A受領・検証・DS015登録。買い83件・期待値R=+0.2262を確認。
  事前予測(約45件)を上回ったため、買い取引数Guardrailの達成可否はBの削減率次第となった。
  Bの結果待ちのため判定は未確定）
